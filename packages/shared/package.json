{
  "name": "@door-in-four/shared",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@door-in-four/config": "file:../config",
    "@door-in-four/types": "file:../types",
    "@supabase/supabase-js": "^2.46.1",
    "stripe": "^17.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "vitest": "^2.1.3"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint not configured yet'",
    "build": "echo 'build not configured for this package'"
  }
}
