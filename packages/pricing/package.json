{
  "name": "@door-in-four/pricing",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@door-in-four/types": "file:../types"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint not configured yet'",
    "build": "echo 'build not configured for this package'"
  }
}
